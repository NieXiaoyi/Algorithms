# 排序
## 位图排序
适用场景：数据量大，无重复数据的排序场景，时间复杂度O(n)
## 归并排序
时间复杂度O(n^2)
### 自顶向下的归并排序（递归）
1. 将数组一分为二，当两个子数组排序完成后，把两个子数组合并在一起
2. 对每个子数组重复步骤1
### 自底向上的归并排序
1. 将每个数当做一个最小的子数组，不断合并，直至最大的数组合并完成
## 快速排序
快速排序使用分而治之的方法：
1. 取数组首元素，对数组进行分区，将该元素放置于两分区中间，使得左分区元素均小于该元素，右分区元素均小于该元素
2. 递归对左右分区数组重复执行步骤1操作

时间复杂度O(n*logn)